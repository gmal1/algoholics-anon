function quickSort(arr: number[]): number[] {
  return arr;
}

function swap(arr: number[], i: number, j: number): void {
  [arr[i], arr[j]] = [arr[j], arr[i]];
}

function partition(arr: number[], i: number, j: number): void {}
